{
  "name": "e_complex",
  "version": "0.1.0",
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "start": "lerna run start",
    "lint": "lerna run lint",
    "build": "lerna run build",
    "bootstrap": "lerna bootstrap"
  }
}
